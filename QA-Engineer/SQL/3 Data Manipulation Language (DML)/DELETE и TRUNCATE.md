1. `DELETE` используется для удаления 1-N строк в таблице, а `TRUNCATE` для удаление всех данных в таблице
2. После `DELETE` можно восстановить данные, а после `TRUNCATE` нельзя (исключение SQL Server)
3. `DELETE` - DML команда, а `TRUNCATE` - DDL
4. Оператор `TRUNCATE` работает быстрее