Это можно сделать с помощью инструкции `ALTER TABLE`, как показано ниже:

```
ALTER TABLE table_name
    ALTER COLUMN col_name TYPE new_data_type;
```

где `new_data_type` — любой тип, например `varchar(30)` или `bigint`